# OpenRewrite Playbook

Compatibility note: deterministic execution guidance now lives primarily in
`java-migration/SKILL.md`.

Use this file only as a short pointer when an older prompt or workflow refers to
"the OpenRewrite playbook".

Primary source of truth:

- `java-migration/SKILL.md`

Resources:

- `references/openrewrite/maven-openrewrite-notes.md`
- `references/openrewrite/presets/*.json`
- `scripts/openrewrite/run-openrewrite.sh`
- `scripts/openrewrite/register-openrewrite-result.py`

Keep wave outcomes and residual issues in `docs/java-migration/PLAN.md`.
