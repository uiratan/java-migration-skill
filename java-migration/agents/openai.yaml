interface:
  display_name: "Java Migration"
  short_description: "Single self-contained skill for Java migration strategy and execution"
  default_prompt: "Use $java-migration as the self-contained entry skill for this repository: infer the operating mode, use the bundled bootstrap, state, discovery, planning, OpenRewrite, and stabilization helpers, bootstrap docs/java-migration only when requested or required by persisted state, load only the needed playbook under references/, keep a strict context budget with a mandatory handoff at 40% and a hard ceiling at 50%, and update the official handoff and state with a ready-to-send next-session prompt."
