interface:
  display_name: "Migration Orchestrator"
  short_description: "Main entry skill for migration strategy, diagnosis, and execution"
  default_prompt: "Use $migration-orchestrator as the self-contained entry skill for this repository: infer the operating mode, use its bundled scripts/bootstrap and scripts/state helpers, bootstrap docs/java-migration only when requested or required by persisted state, load only the needed playbook under references/, and update the official handoff and state."
